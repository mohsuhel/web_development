// write the which is the biggest among those nmbr using operators, comparsion operations(>,<,>=,<=,=!),logical(&&,||)

// let is a keyword used for avoiding hoisting and re declartaion

let x = 10;
let y = 20;

if(x>y && x==y) //comparison opertor nd logicl operator  is used
{
    console.log("x is greater than y")
}
else
{
    console.log("y is greater than x")
}